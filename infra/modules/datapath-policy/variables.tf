variable "cell_id" {
  type        = string
  description = "Documented for parity with the other cell modules; the NetworkLogging object is cluster-scoped and singleton (`default`), so nothing interpolates it."
}
