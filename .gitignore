node_modules
dist
*.local
.env
.DS_Store
tsconfig.tsbuildinfo

CLAUDE.local.md

# Terraform working dirs (provider binaries) — lock files ARE committed
.terraform/
# Local Terraform state (real state lives in gs://<project>-tfstate; never commit)
*.tfstate
*.tfstate.*
tfplan
.auto.tfvars
