import { Icon } from "@/design-system/icon";

/** The Steloit mark — hexagon in steel. */
export function Mark() {
  return (
    <span className="mark">
      <Icon id="s-hex" className="h-full w-full" />
    </span>
  );
}
