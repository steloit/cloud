{
  "name": "@steloit/canon",
  "version": "0.1.0",
  "description": "The one demo world (docs/product/19-canon, ADR-026) + arithmetic invariants as test utils. Import this; never read fixtures.json raw.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./fixtures.json": "./src/fixtures.json"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
