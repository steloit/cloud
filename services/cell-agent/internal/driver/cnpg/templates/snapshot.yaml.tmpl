apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshot
metadata:
  name: {{.SnapshotName}}
  namespace: {{.Namespace}}
spec:
  volumeSnapshotClassName: pd-cell-snapclass
  source:
    persistentVolumeClaimName: {{.Source}}-1
