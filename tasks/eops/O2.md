---
id: O2
title: Cost guardrails: billing alerts at 50/80/100% of trial credit
epic: EOPS
status: stub
phase: MVP
priority: high
sprint: 1
estimate: 0.25ew
deps: [P1]
issue: 181
labels: [DevOps]
module: Cross-cutting
contexts: [provisioning]
files: []
verify: []
owner: agent
---

## Goal

Cost guardrails: billing alerts at 50/80/100% of trial credit

## Summary

Plus: §5 price re-verification reminder before any financial decision after ~2026-10-07.

## Acceptance criteria

- [ ] alerts fire to founders; spend dashboard bookmarked.

> **Stub** — run the spec-author skill to enrich to `ready` before starting. Plan reference: docs/plan/implementation-plan.md §5 EOPS.

## Discovered state (O24, 2026-08-23) — read this before enriching

Gathered with live `gcloud` against project `steloit-dev`, billing account
`016006-61AFB9-0DD7E7`. This task was never started; its status said otherwise.

**A budget already exists**, and it is not the one this task describes:

| | this task asks for | what exists (`steloit-dev tripwire`) | terraform module |
|---|---|---|---|
| instrument | % of the **$300 trial credit** | **₹1000 per calendar month**, project-scoped | monthly budget |
| thresholds | 50 / 80 / 100 % | **50 / 90 / 100 %** | 50 / 80 / 100 % |
| notification | "alerts fire to founders" | `notificationsRule: {}` — no Pub/Sub, no explicit channel; relies on GCP's default email to billing-account admins | `alert_emails` var |

`INCLUDE_ALL_CREDITS` is set, so the live budget counts trial credit against the
₹1000 rather than tracking the credit balance.

So **terraform, this task, and live GCP disagree on all three of the instrument,
the amount and the middle threshold.** Enrichment has to resolve that, and the
resolution is a founder decision about what the guardrail is for: a monthly burn
tripwire and a trial-credit exhaustion alarm are different alarms with different
right answers.

Note the terraform module here (`infra/modules/cost-guardrails`) is the one whose
duplicated call kept CI's infra job red until O22.

**Also unresolved:** the acceptance criterion "spend dashboard bookmarked" is not
a checkable `verify:` command. Enrichment should replace it with something
executable or move it out of the AC.
