---
id: T5.7
title: Docs: API reference, CLI reference, partner onboarding guide
epic: E5
status: done
phase: V1
priority: medium
sprint: 7
estimate: 0.5ew
deps: [T2.8]
issue: 91
labels: [Documentation]
module: M10 Clients
contexts: [api-conventions]
files:
  - docs/dev/**
  - examples/**
verify:
  - test -s docs/dev/api.md && test -s docs/dev/cli.md && test -s docs/dev/getting-started.md
owner: agent
---

## Goal

Docs: API reference, CLI reference, partner onboarding guide

## Summary

API reference generated from openapi.yaml; CLI reference from help output; onboarding guide versioned with releases.

## Acceptance criteria

- [x] Partners onboard from the guide alone: getting-started walks token → auth →
      project → estimate-first create → bind → deploy → events -f (CLI and SDK), honest
      about the P1 boundary; a runnable Node example ships in examples/node-alpha-loop.
- [x] API reference covers exactly the 45 live operations (source of truth:
      oapi-server.cfg.yaml include-operation-ids) with the error model, estimate gate,
      pagination reality, and SSE documented once; CLI reference matches the registered
      command set. Every doc carries a Sources: line so drift is auditable.
- [ ] Regeneration in CI on yaml change — deferred: the reference is hand-curated over
      generated sources for v0; a docs-drift CI check is the follow-up.

## Outcome

- First Docs-agent delegation of the Phase-2 loop: the agent drafted within its write
  scope (docs/dev/**, examples/**), the builder reviewed and committed.
- The audit surfaced real drift beyond docs (reported, fixed in follow-ups): the CLI's
  problem renderer still parses the pre-conformance body shape (reasons strings /
  denied_by / retry_after_s) — non-conforming since #234; a `--ha` bool-flag parsing
  bug; base-URL default drift (spec says api.steloit.app, clients default
  api.steloit.com); the SDK needs a build step before plain-Node consumption.
