---
id: T5.3
title: Alpha nouns: project env db bind deploy logs token init connect (+dev stub)
epic: E5
status: done
phase: MVP
priority: high
sprint: 5
estimate: 1.25ew
deps: [T5.1, S11]
issue: 87
labels: [CLI]
module: M10 Clients
contexts: [api-conventions]
files:
  - apps/cli/internal/cli/**
verify:
  - cd apps/cli && go build ./... && go vet ./... && go test ./...
owner: agent
---

## Goal

Alpha nouns: project env db bind deploy logs token init connect (+dev stub)

## Summary

**AC:** every alpha-path step executable by CLI alone (the D11 loop end-to-end). **ADR-039/040:** include `steloit add <intent>` (app, database, jobs, search, vector, cache) — the intent catalog's CLI door: prints the Composer's named resolutions + estimate, provisions only on accept (gated on the S11 intent surface).

## Acceptance criteria

- [x] Alpha nouns live as renders of contract operations: project (create/list/inspect),
      env (create/list), db (create/list/inspect/destroy), bind, deploy (—/list/rollback),
      token (create/list/revoke), events, init (T5.1), connect (T5.2); logs and dev are
      honest stubs pointing at E6/E4 (shown with the reason, never hidden — §7).
- [x] The §3 safety grammar enforced: db create ALWAYS shows the estimate lines + total
      before the prompt; --yes accepts a shown estimate (no flag skips seeing it);
      destroy requires --confirm <exact-name> with blast radius + the 30-day recovery
      path stated, and no --force exists; token create prints the secret exactly once
      (stdout) with the U7 warning (stderr).
- [x] Context worn, not guessed: creates echo `prj/production ·`; env names resolve via
      the project's environment list; omitted env = production (ADR-037).

## Outcome

- Every command goes through the generated client (`packages/contracts/go`) and the
  shared output layer — no hand-written HTTP, no per-command output conventions.
- Tested against a scripted fake of the /v1 surface: declined prompts create nothing;
  --yes still prints the lines; wrong confirm names refuse; quiet/json modes verified;
  canon $24/$208 render through the money helper.
- `env delete` awaits T4.7 (DELETE /envs/{env} is a recorded contract gap); org keys
  (`key`) ride when their scope model resolves; ask/metrics/alert/template/policy nouns
  arrive with their epics.
