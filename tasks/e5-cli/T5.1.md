---
id: T5.1
title: CLI skeleton (language per SP0-2; generated core per sdk.md)
epic: E5
status: done
phase: MVP
priority: high
sprint: 3
estimate: 0.75ew
deps: [SP0-2]
issue: 85
labels: [CLI]
module: M10 Clients
contexts: [api-conventions]
files:
  - apps/cli/**
  - .github/workflows/ci.yml
verify:
  - cd apps/cli && go build ./... && go vet ./... && go test ./...
owner: agent
---

## Goal

CLI skeleton (language per SP0-2; generated core per sdk.md)

## Summary

**AC:** `steloit <noun> <verb> [--org/--project/--env]` routing; context resolution flags → repo link → profile default.

## Acceptance criteria

- [x] `steloit <noun> <verb> [--org/--project/--env]` routing: registry-dispatched
      (grammar from cli.md §6 only), flags anywhere, §4 exit codes as constants,
      usage/help paths tested.
- [x] Context resolution flags → repo link → profile default, PER FIELD (a repo link
      that pins only the project still takes the org from the profile); `.steloit`
      searched upward; `STELOIT_CONFIG`/`XDG_CONFIG_HOME` honored; env omitted =
      production forever (ADR-037), said by the echo, never guessed.

## Outcome

- `apps/cli` (Go, per SP0-2/ADR-0001): stdlib-only skeleton — no CLI framework
  dependency; single static binary; `Version` stamped via ldflags.
- Commands: `version` (human + --json) and `init` (writes the `.steloit` repo link —
  GOV-002 §3.5). Auth is T5.2; nouns are T5.3; the full output layer is T5.5.
- Config at `~/.config/steloit/config.json`, 0600 (it will hold the token), with env
  overrides for tests and self-hosting (`STELOIT_API_URL`).
- The context Echo renders the worn crumb (`shop/production ·`) for state-changing
  commands to prefix.
- CI: apps/cli joined the go job (build+vet+test).
