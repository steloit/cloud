---
id: T14.6
title: Backups list + branches list/create
epic: E14
status: stub
phase: V1
priority: medium
sprint: 16
estimate: 1ew
deps: [S2]
issue: 168
labels: [Backend, Database, API]
module: M12 Data-plane depth
contexts: [api-conventions, provisioning]
files: []
verify: []
owner: agent
---

## Goal

Backups list + branches list/create

## Summary

D5/W5; also unblocks CLI `db branch`. Restore-to-branch (never in place) is wave 2.

## Acceptance criteria

- [ ] GET /backups; GET/POST /branches; branch create = snapshot-recovered CNPG cluster (ADR-0003), never exposed as a substrate concept.

> **Stub** — run the spec-author skill to enrich to `ready` before starting. Plan reference: docs/plan/implementation-plan.md §5 E14.
