---
id: T14.1
title: SQL exec (read-committed, statement-timeout) + tables browser
epic: E14
status: stub
phase: V1
priority: medium
sprint: 15
estimate: 1ew
deps: [S2]
issue: 163
labels: [Backend, Database, API]
module: M12 Data-plane depth
contexts: [api-conventions, provisioning]
files: []
verify: []
owner: agent
---

## Goal

SQL exec (read-committed, statement-timeout) + tables browser

## Summary

D1/D2 frames; POST /services/{svc}/queries + GET /tables per spec-change §2a.

## Acceptance criteria

- [ ] yaml first; read-only wave — no DML beyond the sandboxed contract.

> **Stub** — run the spec-author skill to enrich to `ready` before starting. Plan reference: docs/plan/implementation-plan.md §5 E14.
