---
id: T2.6
title: problem+json middleware with required remediation + extension fields
epic: E2
status: stub
phase: MVP
priority: high
sprint: 2
estimate: 0.5ew
deps: []
issue: 52
labels: [Backend, API]
module: M2 Identity
contexts: [api-conventions, rbac, events-spine]
files: []
verify: []
owner: agent
---

## Goal

problem+json middleware with required remediation + extension fields

## Summary

Full x-error-catalog: 422 `errors[]`, 409 `reasons[]`, 402 `required_plan`/`overage_price_cents`, 429 Retry-After, 5xx `event_id`.

## Acceptance criteria

- [ ] every error carries `remediation`; contract test per catalog entry.

> **Stub** — run the spec-author skill to enrich to `ready` before starting. Plan reference: docs/plan/implementation-plan.md §5 E2.
