---
id: T3.1
title: Estimate engine (pricing tables as data; canon numbers as fixtures)
epic: E3
status: done
phase: MVP
priority: critical
sprint: 4
estimate: 1.25ew
deps: []
issue: 61
labels: [Backend, Billing]
module: M4 Provisioning
contexts: [provisioning, api-conventions, canon-testing]
files:
  - services/api/internal/estimates/**
  - services/api/internal/provisioning/estimate_http.go
  - services/api/internal/platform/db/migrations/*_estimates.*
  - services/api/db/queries/estimates.sql
  - services/api/internal/identity/estimates_integration_test.go
verify:
  - cd services/api && go build ./... && go vet ./...
  - cd services/api && go test ./...
owner: agent
---

## Goal

Estimate engine (pricing tables as data; canon numbers as fixtures)

## Summary

Shape → line items. The $208-family canon arithmetic is imported (never retyped) as regression fixtures.

## Acceptance criteria

- [x] canon invariants green against the engine: all 6 canon service shapes price to their exact cents from the IMPORTED fixtures file (never retyped); Σ = 20800 (the $208 invariant); checkout-stack = 12600 ($126).

## Outcome

- `internal/estimates`: pricing tables as DATA (`pricing.json`, embedded) → deterministic
  integer-cents lines; server-defaulted intents (S11: postgres→database, valkey→cache,
  web/worker→app); unknown products/sizes fail loudly naming the field, gpu names the
  ADR-0004 surface.
- Canon regression imports `19-canon/fixtures.json` at test time — every canon shape must
  reproduce its exact cents or CI fails; the $208 and $126 sums are asserted.
- `POST /estimates` live: env-fenced (membership → 404, no probing), 401 anonymous,
  template_id 404 until E9; estimates persist with the priced shapes.
- The provision gate's server half ships now: `Accept(estimate, env)` is one-shot,
  env-matching, liveness-checked — T3.3's createService consumes it; env-less pricing
  previews can never be accepted.
- **Findings (S9 candidates, derived to satisfy ratified numbers, recorded in
  pricing.json itself):** (1) db-main's $58 total with 50 GB storage forces an
  included-storage allowance on Standard/Performance (Dev pays per-GB — db-reports' $24
  proves it); (2) web standard-1 decomposes as $10 service base + $17/instance to hit
  canon's $61@3. Both need explicit ratification; every ratified number reproduces today.
