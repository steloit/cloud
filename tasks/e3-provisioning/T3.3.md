---
id: T3.3
title: Service CRUD + shape jsonb + status machine
epic: E3
status: stub
phase: MVP
priority: high
sprint: 4
estimate: 0.75ew
deps: [T3.2]
issue: 63
labels: [Backend, Database]
module: M4 Provisioning
contexts: [provisioning, api-conventions, canon-testing]
files: []
verify: []
owner: agent
---

## Goal

Service CRUD + shape jsonb + status machine

## Summary

`provisioning|ready|degraded|failed|suspended|deleting` (ADR-024 — not running/deleted); `UNIQUE(env_id,name)`; `idx(env_id,status)`.

## Acceptance criteria

- [ ] state machine transitions tested.

> **Stub** — run the spec-author skill to enrich to `ready` before starting. Plan reference: docs/plan/implementation-plan.md §5 E3.
