---
id: T3.4c
title: "The CNPG driver sizes storage from an unschema'd key, so priced storage_gb is never provisioned"
epic: E1
status: done
phase: MVP
priority: high
sprint: 3
estimate: 0.5ew
deps: []
issue: 0
labels: [Infrastructure, Backend]
module: M1 Substrate
contexts: [provisioning]
files:
  - services/cell-agent/internal/driver/cnpg/**
  - services/api/internal/estimates/**
  - services/api/internal/provisioning/services.go
  - services/api/internal/platform/db/migrations/**
  - services/api/internal/identity/services_integration_test.go
  - services/cell-agent/internal/render/**
  - contexts/provisioning.md
  - docs/founder-config.md
  - tasks/e1-substrate/T3.4c.md
  - tasks/e1-substrate/T3.4d.md
  - tasks/e3-provisioning/US-3.11.md
verify:
  - "cd \"$(git rev-parse --show-toplevel)/services/cell-agent\" && go build ./... && go vet ./... && go test -race ./..."
  - "cd \"$(git rev-parse --show-toplevel)/services/api\" && go build ./... && go vet ./... && go test -race ./... # ./... — the contract-identity gate lives in internal/identity and a narrower scope missed a blocker"
owner: agent
---

## The defect

`cnpg.storageForShape` sizes the PVC from `shape["size"]` and a `shape["storage"]`
key — but `storage` is NOT in the API's closed shape schema
(`estimates.shapeSchema` allows `storage_gb`), so the API rejects it and the
driver never sees it. The **priced** `storage_gb` is never read.

A customer billed for 78 GB receives the size-derived default (10Gi for `dev`).

## Why it matters

US-3.7 made the API guarantee that what provisions is what was priced. This
breaks that guarantee one layer down: the contract is enforced at the gate and
then discarded by the renderer. Being right at the API and wrong at the cell is
worse than being wrong consistently — the invoice and the audit trail both say
the customer got what they paid for.

## Investigated 2026-08-23 — the defect is WIDER than filed, and one part needs a ruling

`storageForShape` is twelve lines and contains **three** independent defects, not
one. Measured against `pricing.json`, which is the catalog:

| size | `included_gb` (priced) | PVC rendered today |
|---|---|---|
| `dev` | 0 | 10Gi |
| `standard` | **50** | **32Gi** |
| `performance` | **50** | **10Gi** (no case — falls through `default`) |
| `pro` | *not in the catalog* | 128Gi (dead code) |

1. **The filed defect.** `shape["storage"]` is not in `estimates.shapeSchema`
   (which allows `storage_gb`), so the API rejects it and the driver never sees
   it. The priced `storage_gb` is never read.
2. **`standard` renders 32Gi while its size alone includes 50 GB.** Under-
   provisioned *before* any extra storage is purchased — this one does not need
   `storage_gb` to be involved at all.
3. **`performance` has no case**, so the most expensive size (11200¢, 50 GB
   included) gets the **smallest** volume. `case "pro"` is dead: no such size
   exists.

### RULED 2026-08-23 (founder): unset means the size's `included_gb`, not 0

> "The customer-facing pricing definition takes precedence. When `storage_gb` is
> unset for a `standard` Postgres, the effective included storage is **50 GB**.
> Do not interpret unset as 0."

So `included_gb` is storage the customer **GETS**. Of the two readings below the
first is the ruling; it is implemented at the **API**, not the driver, because
`included_gb` is a pricing concept and the cell-agent is a separate module that
must not carry a copy of the catalog. Resolving it at the gate also satisfies
US-3.7's law directly: the shape a customer is priced on — and the one persisted
and shipped to the cell — says 50 rather than implying it.

### The reading that was open before the ruling

`storage_gb` defaults to **0** in the shape schema, and `included_gb` is a
*pricing* concept the cell-agent cannot see (it must not import the API's pricing
table — separate module, and duplicating the table is how it drifts).

So: **what does a `standard` with `storage_gb` unset actually receive?** The
price says 50 GB is included. The AC below says "sized from the priced
`storage_gb`", which for an unset field is 0. Those cannot both be satisfied.

Two coherent answers, and the choice is the founder's because it decides what
"included" means commercially:

- **`included_gb` is storage the customer GETS** → PVC = `max(storage_gb, included_gb)`,
  and the control plane must resolve that (it is the side that knows the catalog),
  passing an explicit size in the `Spec` rather than leaving the driver to guess.
- **`included_gb` is only "not charged extra"** → PVC = `storage_gb`, and the API
  should default `storage_gb` to the size's included amount so the contract is
  explicit at the gate rather than implied at the cell.

The second is more consistent with US-3.7's law (what provisions is what was
priced, and the priced shape is the contract). Either way the fix belongs partly
in the **control plane**, which is why this task's `files:` already includes
`services/api/internal/provisioning/services.go`.

### Not blocked on that ruling

Defects 2 and 3 are wrong under *both* readings — `standard` and `performance`
are under-provisioned relative to what their own base price includes, with no
`storage_gb` involved. Those can land first.

## Acceptance criteria

- [x] The PVC is sized from the priced `storage_gb`.
- [x] `size` continues to determine the compute class only.
- [x] Ground-truth parity: the T3.4 parity tests still pass unchanged.
- [x] Mutation-verified: reverting to the `storage` key is RED.
- [x] `storage` is removed rather than left dual-read.
- [x] `performance` is handled and `pro` is removed — the size vocabulary is
  asserted against the catalog, and mutating `pricing.json` turns the driver RED.
- [x] A test asserts every size in `pricing.json` renders a PVC at least its
  `included_gb`, reading the file rather than a retyped list.

## Outcome

Three defects, one ruling, both planes — plus two the review found in the fix.

**The rule is `max(declared, included_gb)`, not "included when unset".** The first
implementation resolved only the ABSENT case, which made `{size: standard}` and
`{size: standard, storage_gb: 0}` two different contract identities and turned
`TestEstimateGateRefusesAPriceCollidingShape` red on CI: the estimate gate is
built on "the same configuration spelled with its defaults written out must not be
refused". It also left the incoherence this task exists to close — the declared
shape saying 0 while the volume was 50. A customer cannot receive less than the
size includes, so 0, 30 and 50 on a standard are ONE contract. Rejecting a
below-included value instead would have made every pre-existing standard row
permanently un-PATCHable, since their stored shape holds `storage_gb: 0` and
`UpdateService` re-resolves the merged shape.

**Teardown must not be able to fail for a sizing reason.** Refusing an
uncatalogued size is right for CREATE, but the same `Render` call served
teardown, so such a service became UNDELETABLE — its cluster kept running and
`UpdateService` refuses a row already `deleting`. Reachable by ordinary deploy
skew: the API accepts a size the moment it is in `pricing.json`, while a cell on
the previous agent binary has no `includedFloorGB` entry. The cross-module
catalog test cannot see it, because it binds the two FILES, not the two BINARIES.
`Driver.Objects` now names what a service owns without rendering it, and a test
pins that create still refuses what teardown tolerates.

**API** (`estimates.resolve`): an unset postgres `storage_gb` now resolves to the
size's `included_gb` (founder 2026-08-23). The price does not move — `Price`
charges `storage_gb - included_gb` only when positive, so 50 - 50 = 0 — and a
test pins that against the catalog's own base_cents, because a defaulting rule
that re-prices existing customers is a billing incident, not a rendering one.
`Canonical` treats unset and explicit-50 as the SAME contract, so a customer who
spells out what they were given can still provision the estimate they were
quoted. A value BELOW the included amount resolves up to it: you cannot receive less
than the size includes, so 0, 30 and 50 on a standard are one contract. (An
earlier draft of this Outcome said "an explicit 0 is left alone", which
contradicted the paragraph above it and described the implementation the
architecture review rejected.)

**Driver** (`cnpg.storageForShape`): sizes from `storage_gb`, floored at the
size's included amount. `storage` — never in the API's closed schema, so the
priced value was never read — is removed, not dual-read. `pro` (no such size) is
gone, `performance` (11200¢, 50 GB included, previously rendering the *smallest*
volume) is handled, and an uncatalogued size is REFUSED rather than defaulted.
An ABSENT size is still `dev`, matching the API's schema default — following the
contract, not guessing.

**The catalog is the source.** `TestEveryCatalogSizeRendersExactlyItsIncluded
Storage` reads `pricing.json` from the repo root (the `parity_test.go`
precedent), so the cell-agent binds to the catalog without importing it.
The binding is an EQUALITY, in both directions and over both key sets: mutating
`included_gb` up or down turns the driver RED, so does moving the driver's floor,
and so does either module holding a size the other does not. An earlier version
asserted only `rendered >= included`, which bound one direction — lowering the
catalog and over-provisioning in the driver both survived it.

**Both halves of the wire.** The resolved shape crosses to the cell as JSON, so
the `int` the API resolves arrives as a `float64`. Reading only the Go int would
have proved nothing about the bytes the agent sees; both sides are pinned.

**Two follow-ups filed, not silently absorbed** (protocol §7): **T3.4d** — branch
and PITR restore hardcode `10Gi`, so a branch of a `standard` asks for less than
the snapshot's `restoreSize` and the CSI driver rejects it; this fix widened that
gap from 32→10 to 50→10, and neither entry point is wired yet. **US-3.11** — a
permanently unrenderable service retries forever with nothing written back, which
this task made newly reachable.

**Ten mutations RED**, on harnesses whose no-mutation baseline was asserted GREEN
first: standard floored back to 32 · performance dropped from the table · the
dead `storage` key re-read · float64 ignored · unknown size falling through ·
`included_gb` moved in the catalog · the default removed · unset back to 0 · the
default overwriting an explicit value · included storage charged for.

**One existing test was corrected, not adjusted to pass.** `TestRenderErrorPaths`
asserted that a non-string `size` silently became `dev` — the smallest volume,
which is exactly the under-provisioning this task closes. It now requires a
refusal: `size: 42` reaching the driver means the API's string schema was
bypassed, and answering that with the cheapest PVC is a guess about a contract
we cannot read.

## Found by

US-3.7's architecture review (2026-07-26).

## Related

T3.4 (the CNPG driver) · US-3.7 (the same law at the API) · ADR-025

## Follow-up (2026-08-25): the SIZE-downgrade question is RULED

T3.4c shipped with one open item — `docs/founder-config.md` §5 recorded *"What a
SIZE downgrade costs while storage is retained"* as ❓ NEEDS FOUNDER INPUT, with
three live options. **Ruled by the founder on 2026-08-25:**

> Storage is a ratchet. A PostgreSQL volume may grow but must not physically
> shrink… Pricing must reflect the resulting effective configuration.

Option **(a)** stands: the effective configuration is what is priced. (b)
refuse-the-downgrade and (c) decouple-storage are declined.

### What changed, and what did not

**The behaviour did not change — it was already correct, and unruled.** The PATCH
merge carries the stored `storage_gb` forward, `estimates.Resolve` only ever
RAISES a value below the size's `included_gb`, and an explicit reduction is
refused. So `standard`(50 GB) → `dev` already retained 50 GB and priced
`1900 + 50×50¢ = 4400¢`.

What changed is the **authority**. The integration test's comment said in as many
words that 4400 was *"today's arithmetic… not a ruling"*, and that an earlier
revision calling it "the right answer" on the implementer's authority was the one
thing implementation must never do. That distinction is preserved: the comment
now records a ruling, dated and attributed.

### Where the rule is expressed, and what proves each

| plane | expression | proof |
|---|---|---|
| founder config | §5, ruled row | — |
| pricing engine | `Resolve` raises to `included_gb`, never lowers | mutation: merge dropping `storage_gb` → **RED** |
| control plane | merge preserves it; `refuseStorageShrink` refuses a reduction | mutation: guard removed → **RED** |
| desired state | the doc the CELL receives carries the retained value | mutation: `desiredDoc` stops embedding the shape → **RED** |
| the ratchet itself | storage ABOVE the size floor survives a downgrade | mutation: `Resolve` re-derives from the floor → **RED** (5 tests) |
| legacy rows | a shape with no `storage_gb` is not downgraded to zero | mutation: stored shape not resolved before merge → **RED** |
| driver | renders the retained volume, not the smaller size's default | mutation: driver ignores `storage_gb` → **RED** |
| convergence | successive converges apply the retained size every tick | mutation: driver ignores `storage_gb` → **RED** in `render.TestSuccessiveConvergesNeverAskForASmallerVolume` |

### Review round 1 — three of these rows were weaker than they read

- The desired-state row cited a test that **could not fail** on its own subject.
  `desiredStorageGB` fell back to the stored shape whenever the desired doc had
  no `shape` key — and `desiredDoc`'s `embed` omits the key entirely for an empty
  payload, so deleting `embed("shape", shape)` left the test GREEN while the
  agent, handed a shapeless doc, would resolve size→`dev` and render 10Gi. The
  fallback is gone; the mutation is now RED, measured.
- The convergence row cited five successive calls to `Driver.Render`, which is a
  **pure function** — no state, no clock, no I/O. Five identical calls can only
  catch non-determinism. The property is about successive CONVERGES, so the test
  moved up a layer to `internal/render`, drives `Converge` three times against
  the fake applier, and reads the volume off the Cluster that actually reached
  the API server. It carries a discriminator: a doc whose `storage_gb` is GONE
  must apply a SMALLER volume, or the loop above it proves nothing.
- The retained-volume test's negative control was hardcoded to `standard`/50
  while the loop swept every catalog size, so it did not guard the sizes it
  claimed to. It is computed per size now and skips loudly rather than passing
  quietly where the shrink is not expressible.

### QA round 1 — two MEASURED survivors, one of them a live defect

- **Every downgrade test used `storage_gb: 50`**, which is exactly `standard`'s
  `included_gb` — so "carry the customer's number" and "re-derive from the
  catalog floor" produce the same answer and no test could tell them apart. QA
  measured it: clamping the resolved `storage_gb` down to the old size's floor
  left the **entire api module green**. On `{standard, storage_gb: 200}` that is a
  50Gi PVC against a 200Gi volume the CSI driver refuses, and a bill falling from
  11900 to 4400 — both halves of the ruling violated with nothing red. Covered by
  `TestADowngradeKeepsStorageDeclaredAboveTheSizeFloor`, and the driver sweep now
  renders each size at its floor **and** above it.
- **`refuseStorageShrink` fails open when the stored shape has no `storage_gb`**,
  and the ratchet migration cannot reach a row whose `size` is absent or
  uncatalogued — so on such a row the guard was disarmed permanently. Measured:
  stored `{"size":"standard"}` + PATCH `{"size":"dev"}` was ACCEPTED at resolved
  0, priced 1900 against the ruled 4400, desired 0, 10Gi against a 50Gi volume.
  **Fixed structurally rather than tested around:** `UpdateService` now resolves
  the STORED shape before merging, so "absent keys survive" has a number to
  preserve and the floor comes from the catalog rather than from what the row
  happened to be written with. The guard no longer depends on a fact about
  history. Its `!hadPrior` arm is now reachable only for a shape the catalog
  cannot resolve — which `storageForShape` refuses outright, so no volume exists
  to shrink — and the comment says so instead of passing silently.
- The sub-floor refusal **claimed a physical fact that is false**: below the
  driver's 10Gi minimum, 8 GB and 3 GB render the same volume, so nothing
  shrinks. Canon ships that shape (`svc_jobs`, `storage_gb: 4`). The refusal is
  still right — the recorded figure is what the invoice charges — but for a
  billing reason, not a geometric one, and the message now says which is which.
- The boundaries nothing pinned (upgrade, no-op, `performance`→`dev`, explicit
  equal/above/below) are one table now.

### The invariant, stated — and the suite that proves it rather than exercises it

> Before merging an update, resolve the existing stored configuration to its
> effective/catalogued shape, then merge the requested changes while preserving
> the effective storage floor.

Enforcement is that resolution, **not** whether a migration happened to populate
`storage_gb`. `TestTheStorageRatchetInvariantHoldsForEveryAcceptedPatch` sweeps
the axes that can break independently and asserts the property separately from
each expected value, so a wrong expectation cannot hide a violation:

| stored | patch | effective after | what the case decides |
|---|---|---|---|
| `standard` + 200 | → `standard` | **200** | an above-floor value survives an edit that does not touch it |
| `standard` + 200 | → `dev` | **200** | 50 = standard's floor, 0 = dev's; only 200 is *retained* |
| `standard`, **no** `storage_gb` | → `dev` | **50** | the floor is recovered by resolving the STORED shape |
| **uncatalogued** `pro` + 200 | → `dev` | **200** | resolution FAILS here, so the raw map merges — and must still carry the number |
| **uncatalogued** `pro`, no value | → `dev` | **0** | nothing is provisioned to preserve; accepting is the repair path |
| `dev` + 20 | → `standard` | **50** | the invariant preserves a FLOOR, it does not pin a number |
| `standard` + 200 | → `storage_gb: 120` | **422**, row unmoved | a refusal must not move shape or price |

`TestRepeatingADowngradeConverges` applies the same edit three times and asserts
storage and price both hold — the API half of the convergence clause; the driver
half is `render.TestSuccessiveConvergesNeverAskForASmallerVolume`.

**No value of 50 where it decides anything.** 50 is `standard`'s floor, so
"preserve the customer's value" and "re-derive from the catalog" agree there —
which is exactly why a mutation survived the entire api module. The two rows that
*do* answer 50 are the floor-vs-capacity cases, and they say so.

**Mutation-proved, and precise rather than shotgun** — each kills only what owns it:

| mutation | subtests killed |
|---|---|
| `Resolve` re-derives `storage_gb` from the floor | 5 of 7 — the two survivors are the cases where the floor IS the answer |
| stored shape not resolved before merging | exactly 1 — the no-`storage_gb` row |
| `refuseStorageShrink` returns nil | exactly the two refusal cases |

The uncatalogued-accept path rests on a premise proved elsewhere, not asserted
here: `cnpg.storageForShape` refuses an unknown size, pinned by
`render.TestAServiceWithAnUncatalogedSizeIsStillDeletable`. No PVC exists for such
a row, so there is no volume to shrink.

### The scope of the ruling — and what it did NOT settle

The row this task closed offered three options, and option (a) bundled two
things: the arithmetic **and** "surface a breakdown + consent before the PATCH
commits". The founder ruled the arithmetic. Recording that as "settles option
(a)" would have closed the consent half by implication — on the implementer's
authority, which is the exact defect the previous revision of that row was
written to correct. `PATCH /services/{id}` still reprices and commits in one
call with no estimate and no breakdown; only the hard spend cap stands between
the request and the row. Filed as **US-3.15**, open.

### The gap this closed

Pricing and desired state are ONE property that can diverge: both are computed
from the resolved shape, but only the new test reads what the **agent** actually
receives. Measured in the driver — `{dev, storage_gb: 50}` renders 50Gi, while
`{dev, storage_gb: 0}` (what re-deriving from the smaller requested size
produces) renders 10Gi, which the CSI driver refuses. A downgrade that dropped
`storage_gb` from desired would therefore have **priced correctly and stranded
the service**, outstanding forever with nothing written back. Nothing tested that
until now.

The driver test also carries a negative control: if re-deriving ever stops being
*smaller* than the retained value, the assertions prove nothing, and it fails
loudly rather than passing vacuously.
