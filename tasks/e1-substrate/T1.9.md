---
id: T1.9
title: "founder-config §2 marks resources ✅ created that do not exist in steloit-dev"
epic: E1
status: done
phase: MVP
priority: high
sprint: 2
estimate: 0.25ew
deps: []
issue: 0
labels: [Infrastructure, DevOps]
module: M1 Substrate
contexts: [provisioning]
files:
  - docs/founder-config.md
  - infra/README.md
  - tasks/e1-substrate/T1.9.md
verify:
  - node scripts/spec-sync/validate.mjs
owner: agent
---

## Goal

Make `docs/founder-config.md` §2 describe the project that exists.

## Why

§2 says *"The **dev** foundation is bootstrapped; identifiers below are live."*
Most of it is not. Measured against `steloit-dev` on 2026-08-25, read-only:

| §2 row | says | live |
|---|---|---|
| Terraform state `gs://steloit-dev-tfstate` | ✅ **created** | **exists** |
| KMS key ring + `secrets` key | ✅ **created** | ring `cell-dev-core` **exists** |
| Artifact Registry `images` repo | ✅ **created** | **no repositories in the project** |
| WAL buckets `-wal-customer`, `-wal-control` | ✅ **created** | **absent** |
| Artifacts bucket `steloit-dev-artifacts` | ✅ **created** | **absent** |
| WIF pool `github-ci` + provider `github-oidc` | ✅ **created** | only the GKE-managed `steloit-dev.svc.id.goog` pool |
| SAs `ci-terraform-plan`, `ci-image-push` | ✅ **created** | only the default compute SA |

Commands, so this is re-runnable rather than a claim:

    gcloud storage buckets list --format="value(name)"        # steloit-dev-tfstate, and nothing else
    gcloud artifacts repositories list                        # empty
    gcloud iam service-accounts list --format="value(email)"  # 850447601666-compute@... only
    gcloud iam workload-identity-pools list --location=global # only GKE's steloit-dev.svc.id.goog
    # Added after review: the three measurements that were NEW in this task had no
    # recorded command, and two of them carried the whole causal conclusion.
    gcloud kms keyrings list --location=us-central1           # cell-dev-core
    gcloud kms keys list --location=us-central1 --keyring=cell-dev-core   # secrets-envelope
    gcloud storage cat gs://steloit-dev-tfstate/dev/default.tfstate | jq '.resources[].type'
    gcloud services list --enabled --format="value(config.name)" | wc -l  # 42

This is the same defect class the mistake bank calls *rendered / stored /
enforced are three representations* — here it is **declared vs live**, and the
declared side is a founder-owned file that other tasks cite as fact. Two have
already leaned on it: T1.7's option (b) is priced partly against the "zero static
keys (D5)" CI identity, which does not exist; and it explains why T1.7's missing
API went unnoticed through bootstrap — `ci_plan_viewer`, the first consumer, was
never applied.

## What is proven and what is not

**Proven:** the resources above are absent from `steloit-dev` today.

**Not established, and the task should not assume it:** *why*. Three candidates,
distinguishable by evidence the implementer should gather before editing —
(i) `project_base` was only ever partially applied (the bootstrap `-target` step
stopped after the state bucket); (ii) it was applied and later destroyed;
(iii) the rows were written from the *plan* rather than from the result. The KMS
ring exists while the buckets do not, which is not obviously consistent with any
single story, so look before writing.

## Acceptance criteria

*Restored verbatim after review. An earlier revision of this task rewrote these in
the same commit that ticked them, dropping AC1's second sentence and AC3's
out-of-band clause — which left both conditions ownerless, and AC1 was in fact
violated by the KMS row. Rewriting acceptance criteria to the shape the work took
is the one edit a done-flip must not make.*

1. [x] Every ✅ in §2 either matches live state or is restaked as ⏳/❓ with a note.
   **No row asserts a resource that is not there.** — review caught the KMS row
   claiming an unchecked key; it was scoped to the ring, and then the key was
   actually checked (2026-08-25, credentials restored). **Both exist.**
2. [x] The row for anything not created says what has to happen to create it — §2's
   bootstrap procedure is supposed to be repeatable, and this is the evidence it
   has not been re-run since.
3. [x] The state-file observation is recorded: `gs://steloit-dev-tfstate/dev/default.tfstate`
   holds far less than §2 claims. **If the ring exists outside Terraform's state,
   say so — an out-of-band resource is a fourth representation.** — it does, and §2
   now says so as **established fact**, with the two `terraform import` commands an
   operator needs before the bootstrap can run.
4. [x] Whatever the cause turns out to be, §2 gains a line saying **when** it was last
   verified against live, so the next reader knows the age of the claim.

## Notes

Found by the T1.7 review, confirmed independently. `docs/founder-config.md` is
founder-owned, but correcting a factual record of live state to match live state
is not a decision — it is the opposite of one. Anything that would change an
*intent* (dropping a resource, changing a name) is a decision and stays out.

## Outcome

**Founder-config now describes the project that exists.** Five rows were restaked
from ✅ **created** to ⏳ **absent**, and two were corrected.

| row | was | now |
|---|---|---|
| Artifact Registry (bucket + `images` repo) | ✅ created | ⏳ **absent** |
| WAL buckets (`-wal-customer`, `-wal-control`) | ✅ created | ⏳ **absent** |
| Workload Identity Federation (pool, provider, 2 SAs) | ✅ created | ⏳ **absent** |
| GitHub repo WIF binding | ✅ | ✅ *in code* · ⏳ *not applied* |
| KMS key ring | ✅ created, named `core` | ✅ created, named **`cell-dev-core`** |
| Terraform state bucket | ✅ created | ✅ created, **verified** |
| APIs enabled (13 enumerated) | ✅ enabled | points at `local.services`, the one list |

### What was NOT done, on purpose

- **No history erased.** Every restaked row says what it used to claim and when
  that changed. A row that reads "was ✅ created until 2026-08-25" is recoverable;
  a deleted row is not.
- **No intent changed.** The WIF row still states "zero static keys (D5)" and now
  says explicitly that D5 remains the standing rule — the row records that the
  mechanism is unbuilt, never that keyless CI was abandoned. Same for the WAL
  buckets' separation requirement (INF-001 #10), which is unaffected by the
  buckets not existing yet.
- **Current state and historical evidence are kept apart.** The bootstrap
  paragraph now points at `infra/README.md` §Bootstrap as the contract, and keeps
  the procedure actually *used* below it, marked historical — because that
  paragraph is the evidence for T1.7's defect and deleting it would delete the
  explanation.

### The cause — ESTABLISHED 2026-08-25, once credentials were restored

The first write-up claimed this was established when it was not; review blocked
that, and it was downgraded to an inference with a named anomaly. The anomaly is
now resolved, and it **refutes** the original story rather than confirming it.

Measured, live:

| observation | value |
|---|---|
| state files in `gs://steloit-dev-tfstate` | **exactly one**, `dev/default.tfstate` |
| resources in it | **one** — `module.project_base.google_storage_bucket.state` (serial 47) |
| KMS ring created | `2026-07-19T14:13:58Z` |
| KMS crypto key created | `2026-07-19T14:14:00Z` — **two seconds later** |
| both names | exactly what `project-base` renders (`"${var.cell_id}-core"`, `"secrets-envelope"`) |

Two seconds apart, with the key depending on the ring, is an **apply** — not
console clicks. So the bootstrap's local-state apply on 2026-07-19 reached at
least KMS, and the `-migrate-state` step carried only the state bucket into GCS.

**The original inference was wrong.** The apply did not stop after the state
bucket; it got further, and what was lost is *state*, not resources. Recording
that plainly, because the earlier version of this section reasoned from one
observation to a conclusion and then defended it with a second unevidenced claim —
which is the failure the mistake bank calls naming the wrong cause.

**Operational consequence, now in §2:** `apply -target=module.project_base` fails
with `already exists` on `google_kms_key_ring.core` until both KMS resources are
`terraform import`ed. The two commands are written out there.

The ⏳ **absent** rows were re-verified live on the same day and genuinely do not
exist, so nothing else needs importing.

### Why this row-set is the one that mattered

`ci_plan_viewer` — the first consumer of the API T1.7 was about — lives in the
same never-applied set. That is why a module unable to complete its own bootstrap
step went unnoticed: the step was never run past its first resource.

### A ✅ still has no expiry

The `last verified` line is a date, not a mechanism. Nothing re-checks it, so it
will go stale again. Making drift detectable rather than merely dated is
**T1.10**.