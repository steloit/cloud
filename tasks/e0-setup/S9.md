---
id: S9
title: "Frame/spec reconciliation for ADR-0004 + ADR-038/039/040 (00-sources microcopy — human-only)"
epic: E0
status: stub
phase: MVP
priority: medium
sprint: 0
estimate: 0.2ew
deps: []
issue: 196
labels: [spec-ruling, Documentation]
module: Cross-cutting
contexts: []
files: []
verify: []
owner: founders
---

## Goal

Reconcile the illustrative product-surface references in the frames/design-spec/derived-docs with ADR-0004 (product surface = postgres/valkey/web/worker; storage & AI are Bindings; queue is a Postgres capability; GPU removed). These are 00-sources microcopy/examples — human-decision only; the *decisions* are already consistent everywhere, this closes the stale *exemplars*.

## Items (found by the 2026-07-18 consistency audit)

1. **Design-Spec C7 (region exception)** uses "GPU Worker (beta)" as the example of a product absent from a home region. GPU is removed — pick a new example (e.g. a region-limited service) or re-cast C7. (`Steloit-Console-Design-Spec.md` §C7, ~L189/L228.)
2. **Design-Spec §Extensibility (~L323–326)** uses **Storage** and **AI Gateway** as the archetypal *capability services* (one endpoint, config-shaped, no count badge). Both are now **Bindings**, not services — the "capability service" cardinality currently has no live product example (postgres/valkey/web/worker are all fleet-shaped). Decide: keep the concept documented for future products, and re-illustrate (or note "no current capability-shaped product").
3. **Prebuilt dashboards** list "AI Gateway" (`ia.md` L17, Design-Spec §242, dashboards). With AI as a Binding, decide: rename to "AI Usage" (from AI-Binding provider usage) or drop; likewise confirm the Valkey/Cost/Infra set.
4. **new-product.md** capability example (Storage, AI Gateway) — update to reflect the fleet-only current surface + external Bindings.
5. **Gallery X1** (ai-gateway service) → re-cast as an **AI-Binding exemplar** (ties to S6); **C7/S5/D20 GPU** frames → mark GPU-removed or repurpose.
6. **Intent catalog (ADR-038/039/040, added by the 2026-07-18 audit):** C1 catalog frames sell outcome intents (App · Database · Jobs incl. scheduling · Search · Vector · Cache · Storage · AI) resolved by the Composer — reconcile catalog cards/microcopy; and rule the canon representation: fixtures' `storage`/`queue` product values re-cast (assets → Storage Binding line, jobs → Postgres carrying `intent: jobs`) preserving the $208 invariant (couples with S5; console P4 migration executes the ruling).

## Acceptance criteria

- [ ] Each item ruled (edit frame / re-illustrate / document-as-future); 00-sources updated by human decision
- [ ] Repo grep for "AI Gateway"/"GPU Worker" as a *current product* returns only ADRs/records
- [ ] Derived docs (ia.md, new-product.md) consistent with the ruling
- [ ] Intent-catalog microcopy + canon representation ruled (item 6); P4 canon migration unblocked

> **Stub** — founder microcopy pass (like S8). Plan: docs/plan/product-family-review.md §10.
