---
id: P1
title: GCP account + billing + trial credit activated
epic: E0
status: done
phase: MVP
priority: critical
sprint: 0
estimate: 0.1ew
deps: []
issue: 18
labels: [setup, DevOps, Infrastructure]
module: Cross-cutting
contexts: []
files: []
verify:
  - "gcloud projects describe steloit-dev returns ACTIVE"
  - "gcloud beta billing projects describe steloit-dev shows billingEnabled True"
owner: agent
---

## Goal

GCP account + billing + trial credit activated

## Summary

Set up the GCP org/billing account and activate the $300 trial (covers the entire MVP phase; zonal GKE management is permanently free-tier).

## Acceptance criteria

- [x] billing account live; trial credit visible; two projects creatable (control-plane, cell-0).

## Outcome

**Already landed 2026-07-19; this closes a stale task file, not new work.** The
status was never flipped, so the roadmap said `stub` while the infrastructure had
been live for two days — and a session (mine) then reported P1 as *blocking all
E1 substrate* on the strength of it. `docs/founder-config.md` §2 had it right the
whole time; I quoted a cached note instead of the canonical file.

Verified live against `steloit-dev` rather than trusting either document:

```
$ gcloud projects describe steloit-dev --format='value(projectId,projectNumber,lifecycleState)'
steloit-dev   850447601666   ACTIVE
$ gcloud beta billing projects describe steloit-dev --format='value(billingAccountName,billingEnabled)'
billingAccounts/016006-61AFB9-0DD7E7   True
$ gcloud beta billing accounts describe 016006-61AFB9-0DD7E7 --format='value(displayName,open)'
My Billing Account   True
$ gcloud storage buckets list --project=steloit-dev --format='value(name)'
steloit-dev-artifacts  steloit-dev-tfstate  steloit-dev-wal-control  steloit-dev-wal-customer
$ gcloud kms keyrings list --location=us-central1 --project=steloit-dev
cell-dev-core
$ gcloud iam workload-identity-pools list --location=global --project=steloit-dev
github-ci ACTIVE   steloit-dev.svc.id.goog ACTIVE
$ gcloud iam service-accounts list --project=steloit-dev
ci-terraform-plan@…   ci-image-push@…   850447601666-compute@…
```

40 APIs enabled including compute, container, storage, cloudkms, secretmanager,
iam, iamcredentials, sts, dns, artifactregistry, cloudscheduler, billingbudgets,
monitoring, logging. Project creation under the org is demonstrated — four
projects exist, `steloit-dev` among them.

Not done and correctly so: `steloit-cell0` is deliberately deferred
(founder-config §2 — created at Cell-1, to avoid cost now), and **no GKE cluster
is running** (`gcloud container clusters list` is empty). That is the `⏳ next`
row, standing up under T1.4/CK-M1, not a P1 gap — the spike cluster was torn down
to keep the trial burn at ~$0.

One genuine finding, not blocking: `gcloud organizations get-iam-policy 665960555130`
returns **PERMISSION_DENIED** for `hashir@humanetechnologies.in`. Project creation
and all project-scoped work are unaffected, but org-level IAM inspection is not
available to this account. Filed as a note here rather than a task, since nothing
on the roadmap needs it yet; it will matter if org-level policy or a shared VPC is
ever required.
