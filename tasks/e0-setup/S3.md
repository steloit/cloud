---
id: S3
title: Fix mechanical spec defects (invite renew path param; invite DELETE)
epic: E0
status: done
phase: MVP
priority: high
sprint: 0
estimate: 0.1ew
deps: []
issue: 25
labels: [API]
module: Cross-cutting
contexts: []
files: []
verify: []
owner: agent
---

## Goal

Fix mechanical spec defects (invite renew path param; invite DELETE)

## Summary

`/invites/{invite}/renew` is missing its path param (generated client gets `path?: never`); `DELETE /invites/{invite}` is missing entirely.

## Acceptance criteria

- [ ] openapi.yaml fixed; clients regenerate cleanly; ~30-minute edit.

## Outcome

Fixed 2026-07-18: `/invites/{invite}/renew` gains its missing `{invite}` path param (the
generated client's `path?: never` defect); the missing invite DELETE turned out to be the
**admin revoke** — public decline (`DELETE /invites/{invite}`) already existed — so
`DELETE /orgs/{org}/invites/{invite}` (revokeInvite, audited, status → revoked) was added,
completing the invite-status enum's revoked path. Clients regenerate cleanly; console green.
