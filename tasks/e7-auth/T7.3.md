---
id: T7.3
title: Session list + revoke
epic: E7
status: done
phase: V1
priority: medium
sprint: 7
estimate: 0.5ew
deps: [T2.1]
issue: 100
labels: [Backend, Security]
module: M2 Identity
contexts: [rbac, api-conventions]
files:
  - services/api/internal/identity/sessions_http.go
  - services/api/db/queries/identity.sql
  - services/api/oapi-server.cfg.yaml
  - services/api/internal/identity/sessions_integration_test.go
verify:
  - cd services/api && go build ./... && go vet ./... && go test ./...
owner: agent
---

## Goal

Session list + revoke

## Summary

**AC:** /account/sessions surface (P3/P4 frames) fully backed; revoke is immediate.

## Outcome

listSessions + revokeSession added to the strict server (identity-owned, no composition
churn). Reuses T2.1's per-request session resolution — revocation needs no cache
invalidation because there is no cache. The P3/P4 security page is fully backed.
