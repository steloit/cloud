---
id: T4.7
title: DELETE /envs/{env} (preview teardown; missing from the yaml)
epic: E4
status: done
phase: MVP
priority: medium
sprint: 6
estimate: 0.25ew
deps: []
issue: 79
labels: [API, Backend]
module: M5 Deploy
contexts: [provisioning, events-spine]
files:
  - docs/product/08-api/openapi.yaml
  - services/api/internal/provisioning/**
  - services/api/internal/platform/db/migrations/*_env_lifecycle.*
  - services/api/db/queries/projects.sql
  - apps/cli/internal/cli/nouns.go
  - services/api/internal/identity/env_lifecycle_integration_test.go
verify:
  - cd services/api && go build ./... && go vet ./... && go test ./...
  - cd apps/cli && go test ./...
owner: agent
---

## Goal

DELETE /envs/{env} (preview teardown; missing from the yaml)

## Summary

Spec-change §2b; add via the S-process (yaml first, then implement).

## Acceptance criteria

- [x] U6 honored: the API names EVERY existing service as a 409 blocker; the CLI
      (`steloit env delete`) states blast radius + the 30-day recovery path and requires
      `--confirm <exact-name>` with no `--force`.
- [x] Teardown is SCHEDULED, never a hard cascade: deployment history references envs
      ON DELETE RESTRICT and is immutable (DP1) — the reconciler tears down and purges.
- [x] The implicit environment never deletes — identity by FLAG (survives its own
      rename, integration-proven): tearing it down is project deletion (ADR-037).

## Outcome

- S-process executed: yaml first — `DELETE /envs/{env}` (spec-change §2b) AND the
  founder-ratified `PATCH /envs/{env}` rename (ADR-037's escape hatch) added together;
  all clients regenerated (Go server/client, console TS, SDK).
- ADR-037 deepened in the schema: `environments.implicit` marks the BORN env — the flag,
  not the name, carries the identity, so `production` renames freely and the implicit
  protections (never a deletion blocker for the project, never deletable itself) follow
  the row. Backfilled for existing envs.
- Rename audited before→after; teardown lands `env.deletion_scheduled` on the spine;
  billing role denied with the role named.
- CLI `env delete` ships the destroy grammar; SDK reaches both ops via the generated
  `ops` surface (ergonomic wrappers ride the next SDK pass).
