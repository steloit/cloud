# INF-001 third-pass review — 2026-07-13 (findings F1–F4, post-A2)

The review Amendment A3 responds to. Scope: A2's own consistency.
Verified this pass: all eleven A1/A2 base-text stamps placed correctly;
A2.3's ToS sentence sound; A2.4's inclusion of preview environments
consistent; A2.5 resolves the D9 tension without side effects.

**F1 · A2 violated its own A2.1 rule.** The base sections were stamped
but the A1 blocks weren't: A1.4's example domain (superseded by A2.4),
A1.5.10 (re-read by A2.5), A1.2 (extended by A2.2) all stood unwarned.
→ stamps applied in the A3 commit.

**F2 · A2.2's design (a) reduces to design (b).** Triggers can't safely
make network calls; NOTIFY needs a held LISTEN connection to the
compute — the A1.2 problem again. Honest (a) = trigger-outbox whose
insert traverses the WAL = (b) by another ingestion path. → A3.1 (one
candidate family, two ingestion variants; broker fallback unchanged,
last resort).

**F3 · The Rung 0 action item was invisible at the point of execution.**
A2.4's register-the-domain action lived only in the amendment; §4's
Rung 0 row — the table Rung 0 runs from — didn't carry it. → stamped.

**F4 · Undefined stamp verb.** §7's *(extended: A1.8)* predated any
definition of "extended". → A3.2 (two verbs exactly: amended =
superseded/corrected; extended = added-to; no others without a
convention amendment).

Disposition: all four adopted in A3. Open after A3: A1.3 RPO value
(founder ratification) · A2.4 domain registration (Rung 0) · GCP/AWS
credit applications (§4, "apply NOW") · §5 price expiry ~2026-10-07.
