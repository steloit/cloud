# INF-001 re-review — 2026-07-13 (notes R1–R5, post-A1)

The second-pass review Amendment A2 responds to, conducted per §8 after
A1 was adopted. Scope: A1's own soundness + residual document
coherence. A1.1's formula was independently recomputed
($2.5k + 500–1,000 × $14.75 ≈ $10–17.3k ✓); the splice/structure
verified. All A1 resolutions judged sound; five notes filed.

**R1 · The base text lies to a cold reader.** Append-only leaves D6's
cell figure, §2's eight invariants, and D7's presigned URLs standing
uncorrected in place. → A2.1 (stamp convention: mechanical inline
*(amended: …)* markers, applied retroactively, mandatory in the same
commit for all future amendments)

**R2 · The NATS fallback may never be needed.** Two D3-substrate
designs plausibly satisfy A1.2's constraint: producer-side signaling
(the producer's compute is awake at enqueue by definition) and —
preferred — safekeeper/CDC-derived depth signals, since queue inserts
already traverse the WAL stream through Safekeepers we operate: zero
held connections, full branch-coherence. → A2.2 (both recorded;
(b) preferred; evaluation mandatory before the fallback)

**R3 · A1.3's exposure statement was optimistic.** Zonal clusters have
zonal PDs: zone loss exposes the RPO window as well as disk loss; the
alpha ToS sentence must say both. → A2.3

**R4 · The object domain is a security decision, not an example.**
Serving customer-controlled content from a subdomain of the primary
domain shares cookie scope and origin-adjacency with the console; only
a separate registrable domain (the githubusercontent.com pattern)
isolates it. → A2.4 (strengthened to a shape requirement: distinct
eTLD+1, registered during Rung 0)

**R5 · A1.5.10 bent D9's arrow.** "Re-adopt state from cells" reversed
"single source of truth" as written. → A2.5 (authoritative reading:
control DB owns desired state; cells report actual; DR = restore
desired, reconcile against actual)

Disposition: all five adopted in A2; R4 in strengthened form. Open
after A2: A1.3's RPO value remains pending founder ratification;
A2.4's content-domain registration is a Rung 0 action item.
